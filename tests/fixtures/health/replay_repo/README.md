# Replay fixture

Small deterministic repository shape used by the health verification gate.
It contains production code, tests, generated output, vendor code, a manifest,
and CI configuration so scope/exclusion/diff fixtures have stable paths.
